---
// Section 6 — Cream Closing (matcht Section 5 Werkstattlicht-Palette).
//
// Bildet mit Section 5 einen "Ankunfts-Arc": dunkler Fahrplan bricht
// bei Section 5 auf Cream auf, Section 6 fuehrt konsequent im Cream
// weiter und muendet in den einzigen schweren Punkt auf hellem Grund:
// den dunklen Amber-CTA-Block. Kein Video, keine Cosmic-Aurora, keine
// orbital breathing frame — Typography + eine bewusste Farbfrucht.
//
// Content aus SECTION5 constants (eyebrow/headline/subtext/cta bleiben
// gleich, nur visuelle Umsetzung wechselt komplett).

import { SECTION5 } from '../../lib/constants';
import SiteFooter from '../SiteFooter.astro';

// headline + primaryCta.label werden bewusst NICHT destructured —
// Section 6 hardcodet Headline ("Bereit für [Phase 01?]" mit Highlight-
// Span um "Phase 01?") und CTA-Text ("Lass uns sprechen", matcht
// /preise-Sprache), beide nicht via einfacher Interpolation abbildbar.
const { eyebrow, subtext, secondaryEmail } = SECTION5;
---

<section class="section-6" aria-labelledby="closing-headline">
  <!-- Cream BG (matcht Section 5 werkstatt-bg #f7f0dd) -->
  <div class="closing-bg" aria-hidden="true"></div>
  <!-- Blaupausen-Textur (matcht Section 5 werkstatt-blueprint) -->
  <div class="closing-blueprint" aria-hidden="true"></div>

  <div class="closing">
    <p class="closing-eyebrow" data-reveal>{eyebrow}</p>

    <h2 class="closing-headline" id="closing-headline" data-reveal>
      Bereit für
      <span class="closing-highlight">
        Phase 01?
        <svg
          class="closing-highlight-underline"
          viewBox="0 0 200 12"
          preserveAspectRatio="none"
          aria-hidden="true"
        >
          <path
            d="M 3 7 C 50 3, 150 9, 197 4"
            vector-effect="non-scaling-stroke"
          />
        </svg>
      </span>
    </h2>

    <p class="closing-sub" data-reveal>{subtext}</p>

    <div class="closing-cta-wrap" data-reveal>
      <!-- Inline-Link-CTA (matcht /preise Pattern): grosser Text mit
           amber Arrow, subtile amber Hover-Underline. Kein Pill, kein
           Glow — leiseres, editorialeres Statement passend zum Cream. -->
      <a
        class="closing-cta-link"
        href="/kontakt"
      >
        <span>Lass uns sprechen</span>
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <path d="M5 12h14M13 6l6 6-6 6" />
        </svg>
      </a>
    </div>

    <p class="closing-secondary" data-reveal>
      Oder direkt:
      {' '}
      <a href={`mailto:${secondaryEmail}`}>{secondaryEmail}</a>
    </p>
  </div>

  <ul class="trust-trio" aria-label="Was du erwarten kannst" data-reveal>
    <li>
      <svg
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.8"
        stroke-linecap="round"
        stroke-linejoin="round"
        aria-hidden="true"
      >
        <circle cx="12" cy="12" r="10" />
        <polyline points="12 6 12 12 15 14" />
      </svg>
      <span>Antwort &lt; 24 h</span>
    </li>
    <li>
      <svg
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.8"
        stroke-linecap="round"
        stroke-linejoin="round"
        aria-hidden="true"
      >
        <path d="M21 15a2 2 0 01-2 2H7l-4 4V5a2 2 0 012-2h14a2 2 0 012 2z" />
      </svg>
      <span>Erstgespräch gratis</span>
    </li>
    <li>
      <svg
        width="16"
        height="16"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="1.8"
        stroke-linecap="round"
        stroke-linejoin="round"
        aria-hidden="true"
      >
        <circle cx="12" cy="12" r="10" />
        <line x1="2" y1="12" x2="22" y2="12" />
        <path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z" />
      </svg>
      <span>DE &amp; EN</span>
    </li>
  </ul>

  <SiteFooter variant="embedded-cream" />
</section>

<style>
  /* Section-Root: mindestens 100dvh, wachsen erlaubt fuer Footer.
     BG-Layer via absolute-positioned Divs damit Blueprint sauber
     ueber Cream sitzen kann. Cool Cream #efe8d6 (statt Section 5's
     warmer #f7f0dd) erzeugt eine subtile Farbabgrenzung — plus
     amber Top-Hairline als Chapter-Divider fuer klaren visuellen
     Break beim Sticky-Detach von Section 5. */
  .section-6 {
    position: relative;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    background: #efe8d6;
    padding: clamp(100px, 14vh, 160px) 40px 24px;
    isolation: isolate;
    border-top: 1px solid rgba(245, 158, 11, 0.35);
  }

  .closing-bg {
    position: absolute;
    inset: 0;
    background: #efe8d6;
    z-index: 0;
  }

  /* Blaupausen-Textur: gleiches Muster wie Section 5 (32px cross-grid
     in amber-700 5% Opacity) — visuelle Kontinuitaet ueber die
     Section-Grenze hinweg. */
  .closing-blueprint {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(to right, rgba(245, 158, 11, 0.05) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(245, 158, 11, 0.05) 1px, transparent 1px);
    background-size: 32px 32px;
    z-index: 1;
    opacity: 0.9;
    pointer-events: none;
  }

  /* Content Container — zentriert, max-width, wachst flex-1 damit
     Footer natuerlich am Boden sitzt. */
  .closing {
    position: relative;
    z-index: 2;
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    max-width: 820px;
    margin: 0 auto;
    padding: clamp(24px, 5vh, 60px) 0;
    gap: clamp(18px, 3vh, 32px);
  }

  .closing-eyebrow {
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 500;
    font-size: 0.75rem;
    letter-spacing: 0.3em;
    color: #f59e0b;
    text-transform: uppercase;
    margin: 0;
  }

  /* HUGE Typographie — der optische Anker der Section neben dem CTA-
     Block. clamp 3rem → 6rem knallt auf grossen Screens ohne mobile
     zu ueberwaeltigen. */
  .closing-headline {
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 700;
    font-size: clamp(3rem, 8vw, 6rem);
    line-height: 1;
    letter-spacing: -0.035em;
    color: #1a1a1c;
    margin: 0;
    text-wrap: balance;
  }

  /* Highlight-Wort "Phase 01?" — amber-700 mit SVG-Underline,
     matcht die Vertical-Landing-Pages Highlight-Sprache. */
  .closing-highlight {
    position: relative;
    display: inline-block;
    color: #f59e0b;
    white-space: nowrap;
    padding-bottom: 0.08em;
  }

  .closing-highlight-underline {
    position: absolute;
    left: -1%;
    right: -1%;
    bottom: -0.02em;
    width: 102%;
    height: 0.22em;
    overflow: visible;
    pointer-events: none;
  }

  .closing-highlight-underline path {
    stroke: #f59e0b;
    stroke-width: 5;
    stroke-linecap: round;
    fill: none;
    filter: drop-shadow(0 0 6px rgba(245, 158, 11, 0.35));
  }

  .closing-sub {
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 400;
    font-size: clamp(1.05rem, 1.4vw, 1.2rem);
    line-height: 1.55;
    color: rgba(26, 26, 28, 0.72);
    max-width: 560px;
    margin: 0;
  }

  /* Inline-Link-CTA (matcht /preise Pattern, adaptiert fuer Cream):
     Charcoal Text statt light, amber Arrow, subtile amber Underline
     on hover. Groessere Font-Size als Body damit's als CTA lesbar
     bleibt ohne visuell zu dominant zu sein — der Fokus soll auf
     der Headline "Bereit für Phase 01?" liegen. */
  .closing-cta-wrap {
    display: flex;
    justify-content: center;
    margin-top: clamp(8px, 2vh, 16px);
  }

  .closing-cta-link {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: transparent;
    border: 0;
    border-bottom: 1px solid transparent;
    color: #1a1a1c;
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 600;
    font-size: clamp(1.15rem, 1.6vw, 1.4rem);
    padding: 0 0 2px;
    cursor: pointer;
    transition: border-color 200ms ease, color 200ms ease;
  }

  .closing-cta-link svg {
    color: #f59e0b;
    transition: transform 200ms ease;
    flex-shrink: 0;
  }

  .closing-cta-link:hover,
  .closing-cta-link:focus-visible {
    border-bottom-color: rgba(245, 158, 11, 0.6);
    color: #000;
    outline: none;
  }

  .closing-cta-link:hover svg,
  .closing-cta-link:focus-visible svg {
    transform: translateX(4px);
  }

  .closing-secondary {
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 400;
    font-size: 0.92rem;
    color: rgba(26, 26, 28, 0.55);
    margin: 0;
  }

  .closing-secondary a {
    color: #f59e0b;
    text-decoration: none;
    border-bottom: 1px solid rgba(245, 158, 11, 0.3);
    padding-bottom: 1px;
    transition: color 200ms ease, border-color 200ms ease;
  }

  .closing-secondary a:hover,
  .closing-secondary a:focus-visible {
    color: #d97706;
    border-color: rgba(245, 158, 11, 0.7);
    outline: none;
  }

  /* Trust-Trio — Linework-Icons in amber-700, charcoal Labels.
     Horizontal auf Desktop, vertikal auf Mobile. */
  .trust-trio {
    position: relative;
    z-index: 2;
    list-style: none;
    padding: 0;
    margin: clamp(24px, 5vh, 48px) auto 32px;
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 12px 40px;
    max-width: 720px;
  }

  .trust-trio li {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    font-family: 'Neue Montreal', 'Inter', sans-serif;
    font-weight: 500;
    font-size: 0.82rem;
    letter-spacing: 0.02em;
    color: rgba(26, 26, 28, 0.7);
  }

  .trust-trio svg {
    color: #f59e0b;
    flex-shrink: 0;
  }

  /* Mobile Adjustments */
  @media (max-width: 768px) {
    .section-6 {
      padding: 80px 20px 20px;
    }

    .closing {
      padding: 16px 0;
      gap: 16px;
    }

    .closing-headline {
      font-size: clamp(2.4rem, 10vw, 3.4rem);
    }

    .closing-highlight-underline path {
      stroke-width: 4;
    }

    .closing-cta {
      padding: 16px 26px;
      font-size: 1rem;
    }

    .trust-trio {
      gap: 12px 24px;
      margin-bottom: 24px;
    }

    .trust-trio li {
      font-size: 0.78rem;
    }
  }

  /* Reduced Motion — data-reveal wird global von BaseLayout gehandhabt.
     Fuer die Section-spezifischen Transitions Sicherheits-Reset. */
  @media (prefers-reduced-motion: reduce) {
    .closing-cta,
    .closing-cta svg,
    .closing-secondary a {
      transition: none !important;
    }
  }
</style>
